"use strict";

describe("Implicit Grant", function () {
    describe("When a implicit grant request arrives", function () {
        it("should authenticate the resource owner");
        it("should return an access token in the fragment");
        it("should preserve the state from the original request in the redirection");
    });
});